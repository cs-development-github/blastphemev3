{% extends 'panel.html.twig' %}

{% block title %}Mon compte - Modification{% endblock %}

{% block content %}
    {{ form_start(form) }}
        {{ form_row(form.description) }}
        {{ form_row(form.name) }}
        {{ form_row(form.photo) }}
        <button type="submit" class="btn btn-primary my-2">Modifier le compte</button>
    {{ form_end(form) }}
{% endblock %}
