<div class="d-flex flex-row justify-content-evenly">
    <a href="" class="btn btn-light">
        <i class="fas fa-edit"></i>
    </a>
    <form method="post" action="" onsubmit="return confirm('Étes-vous sure de vouloir supprimer?');">
        <input type="hidden" name="_token" value="{{ csrf_token('delete-user' ~ row.id) }}">
        <button class="btn btn-danger">
            <i class="fa fa-trash"></i>
        </button>
    </form>
</div>

{# edit : {{ path('user_edit', {id: row.id }) }}
delete : {{ path('user_delete', {'id': row.id}) }} #}